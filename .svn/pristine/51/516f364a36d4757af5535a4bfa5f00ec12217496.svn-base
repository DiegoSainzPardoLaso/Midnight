#pragma once

#define VULKAN_HPP_NO_CONSTRUCTORS
#include <vulkan/vulkan_raii.hpp>

#include "Descriptors.h"
#include "VulkanImage.h"
#include "RenderNodesManager.h"

#include "../../Inputs/Inputs.h"

struct Picker_VK
{	
	vk::raii::DescriptorSetLayout layout;

	// @note I have to use the RenderNodeSystem data to create this descriptors, there's no need to duplicate the data, just need to draw it with a different slang shader
	// also NOT sure if the array inside a vector is the best thing ever... Luckily this thing is only in the engine and not in the final games (If I evem manage to make one :D)
	//
	std::vector<vk::raii::DescriptorSet[MAX_FRAMES_IN_FLIGHT]> camerasDescriptorSets;
};

// struct PICKER_DATA
// {
// 	int objectType;
// 	int objectIdx;
// 	int triangleIdx;
// };

// namespace fPicker_VK
// {
// 	Picker_VK *Create(const vk::raii::Device &logicalDevice, const vk::raii::DescriptorPool &descriptorPool, const RenderNodeSystem_VK *&renderNodes);
// 	void Update(const Buffer &pickerBuffer, const PICKER_DATA &pickerData);
// 
// 
// 	// PICKER_DATA GetPixelInformation(const Buffer &pickerBuffer, const int x, const int y);
// 	// PICKER_DATA GetPixelInformation(const Buffer &pickerBuffer, const IVec2 &position);
// 
// 	namespace Internal
// 	{
// 		void DrawPickerFrameBuffer();
// 	}
// }

